local config = {
  default_mappings = false,
  default_commands = true,
  disable_diagnostics = false,
  list_opener = "copen",
  highlights = {
    incoming = false,
    current = false,
  },
}

return config
